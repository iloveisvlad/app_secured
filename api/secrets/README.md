# secrets

this directory is intentionally kept without real secrets.

runtime secrets must be provided through environment variables, local .env files excluded from git, or a dedicated secret management mechanism.